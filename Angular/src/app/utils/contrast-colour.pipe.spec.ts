import { ContrastColourPipe } from './contrast-colour.pipe';

describe('ContrastColourPipe', () => {
  it('create an instance', () => {
    const pipe = new ContrastColourPipe();
    expect(pipe).toBeTruthy();
  });
});
